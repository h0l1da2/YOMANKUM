package com.account.yomankum.accountBook.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AccountBookUserRepository extends JpaRepository<AccountBookUser, Long> {
}
